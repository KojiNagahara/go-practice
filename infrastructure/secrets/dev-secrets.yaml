# Secret values should be managed outside source control.
# In production, prefer AWS Secrets Manager or SSM Parameter Store.
Parameters:
  DbMasterUser: admin
  DbMasterPassword: ReplaceWithStrongPassword123!
